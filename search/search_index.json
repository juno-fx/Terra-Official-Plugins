{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Index","text":""},{"location":"#terra-plugin-system","title":"Terra Plugin System","text":"<p>Terra is a modern, scalable, and flexible platform for creating and modifying existing Orion clusters. With hooks allowing for easy integration with the Orion API, Terra is designed to  allow for nearly unlimited customization of your Orion cluster.</p>"},{"location":"#using-plugins-writing-your-own","title":"Using Plugins Writing Your Own","text":""},{"location":"bundles/","title":"Writing Bundles","text":"<p>Bundles are a way to provide multiple plugins in a single package.  This is useful when you have a set of plugins that are related to  each other or want to provide a set of Plugins that represent a larger  install. For example, you may have a set of plugins that all install  software that is used for video editing or software development. You  can then offer it as a single bundle that can be installed with a  single run instead of multiple.</p>"},{"location":"bundles/#prerequisites","title":"Prerequisites","text":"<ul> <li>Read Getting Started</li> </ul>"},{"location":"bundles/#creating-a-juno-pipeline-bundle","title":"Creating a Juno Pipeline Bundle","text":"<p>Juno Innovations provides a default pipeline that it uses for its own projects. This pipeline is offered as a bundle for others to  install and use. We will walk through how this is created.</p>"},{"location":"bundles/#the-plugin-directory","title":"The <code>plugin</code> Directory","text":"<ul> <li>Create a new python file in the <code>plugin</code> directory <pre><code>touch plugins/juno_pipline.py\n</code></pre></li> </ul>"},{"location":"bundles/#setup-run-environment","title":"Setup Run Environment","text":"<ul> <li>Create a new python file in the <code>tests</code> directory <pre><code>touch tests/test_juno_pipeline.py\n</code></pre></li> </ul>"},{"location":"bundles/#writing-the-plugin","title":"Writing the Plugin","text":"<p>Now that we have our plugin file, we can start writing our plugin.</p> plugins/juno_pipeline.py<pre><code>\"\"\"\nJuno's Pipeline Bundle\n\"\"\"\n\n# std\nimport os\nfrom requests import request\n\n# 3rd\nfrom terra import Plugin\nfrom terra.loaders import plugins\n\n\nclass JunoPipeline(Plugin):\n    \"\"\"\n    Git Loader\n</code></pre>"},{"location":"bundles/#configuration","title":"Configuration","text":"Configuration<pre><code>    _version_ = \"1.0.0\"\n    _alias_ = \"Juno Pipeline (Full)\"\n    icon = \"https://avatars.githubusercontent.com/u/77702266?s=200&amp;v=4\"\n    description = \"Install the Juno Pipeline for 2D Visual Effects. Ships with Nuke, Blender, Houdini, and more.\"\n</code></pre>"},{"location":"bundles/#preflight","title":"Preflight","text":"<p>Next we will add a <code>preflight</code> function which will validate the input from the user and ensure that the plugin can run successfully.</p> Preflight<pre><code>    tags = [\"vfx\", \"pipeline\", \"juno\", \"2d\", \"visual effects\"]\n\n    def preflight(self, *args, **kwargs) -&gt; bool:\n        \"\"\"\n        Check if the target directory exists\n        \"\"\"\n        # if not os.path.exists(\"/pipe\"):\n        #     raise ValueError(\n        #         \"The pipeline directory does not exist. Please ensure the pipeline is mounted to /pipe.\"\n        #     )\n\n        # if not os.path.exists(\"/apps\"):\n        #     raise ValueError(\n</code></pre>"},{"location":"bundles/#install","title":"Install","text":"<p>Lastly, we will add the <code>install</code> function which will execute the plugin.</p>"},{"location":"bundles/#calling-other-plugins","title":"Calling Other Plugins","text":"<p>Because the Juno Pipeline is a bundle, it will call other plugins to install the software it needs. This is done by calling the <code>plugins()</code> function that is provided by the <code>terra</code> loaders. This allows Juno to load the Blender or Nuke plugins and install them as needed.</p> <p>This can then be chained together and all versions of the software can be locked by Juno for better support.</p> Install<pre><code>        #     )\n\n    def install(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Run git pull and install to the target directory\n        \"\"\"\n        # ShowPrep Task called DeliveryTemplate\n        delivery_task = {\"code\": \"DeliveryTemplate\", \"parent\": None, \"type\": 1140}\n        luna_url = \"http://luna:8000/\"\n        meta_url = f\"{luna_url}/meta\"\n\n        response = self.get_task(url=meta_url, task=delivery_task)\n        status_code = response.status_code\n\n        if status_code == 200 and not response.json():\n            response = self.create_task(url=meta_url, task=delivery_task)\n        print(response.status_code)\n\n        # handler = plugins()\n        # handler.run_plugin(\n        #     \"plugin\",\n        #     \"Pixelfudger v3.2\",\n        #     allow_failure=False,\n        #     destination=\"/pipe/nuke/external/\",\n        # )\n\n        # handler.run_plugin(\n        #     \"plugin\",\n        #     \"Kdenlive Installer\",\n        #     allow_failure=False,\n        #     destination=\"/apps/kdenlive\",\n        # )\n        #\n        # handler.run_plugin(\n        #     \"plugin\",\n        #     \"Blender Installer\",\n        #     allow_failure=False,\n        #     destination=\"/apps/blender\",\n        #     version=\"4.2.0\",\n        # )\n        #\n        # handler.run_plugin(\n        #     \"plugin\",\n        #     \"PyCharm Installer\",\n        #     allow_failure=False,\n        #     destination=\"/apps/pycharm\",\n        #     version=\"2024.1.4\",\n        # )\n        #\n        # handler.run_plugin(\n        #     \"plugin\", \"ComfyUI Installer\", allow_failure=False, destination=\"/apps/comfyui\"\n        # )\n        #\n        # handler.run_plugin(\n        #     \"plugin\",\n        #     \"Nuke Installer\",\n        #     allow_failure=False,\n        #     destination=\"/apps/nuke\",\n        #     version=\"Nuke15.1v1\",\n        # )\n\n    def get_task(self, url, task):\n        \"\"\"\n        get a task from luna\n        \"\"\"\n        url = f\"{url}/filter\"\n        response = request(\"post\", url, json=task)\n        return response\n\n    def create_task(self, url, task):\n        \"\"\"\n        create a task in luna\n        \"\"\"\n        print(\"CREATING TASK\")\n        task[\"metadata\"] = {\"TemplateType\": \"Delivery\"}\n        print(task)\n        return request(\"post\", url, json=task)\n\n    def uninstall(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Uninstall the application.\n        \"\"\"\n        self.logger.info(\"Uninstalling not implemented\")\n</code></pre>"},{"location":"bundles/#final-code","title":"Final Code","text":"Final Plugin Code<pre><code>\"\"\"\nJuno's Pipeline Bundle\n\"\"\"\n\n# std\nimport os\nfrom requests import request\n\n# 3rd\nfrom terra import Plugin\nfrom terra.loaders import plugins\n\n\nclass JunoPipeline(Plugin):\n    \"\"\"\n    Git Loader\n    \"\"\"\n\n    _version_ = \"1.0.0\"\n    _alias_ = \"Juno Pipeline (Full)\"\n    icon = \"https://avatars.githubusercontent.com/u/77702266?s=200&amp;v=4\"\n    description = \"Install the Juno Pipeline for 2D Visual Effects. Ships with Nuke, Blender, Houdini, and more.\"\n    category = \"Pipeline\"\n    tags = [\"vfx\", \"pipeline\", \"juno\", \"2d\", \"visual effects\"]\n\n    def preflight(self, *args, **kwargs) -&gt; bool:\n        \"\"\"\n        Check if the target directory exists\n        \"\"\"\n        # if not os.path.exists(\"/pipe\"):\n        #     raise ValueError(\n        #         \"The pipeline directory does not exist. Please ensure the pipeline is mounted to /pipe.\"\n        #     )\n\n        # if not os.path.exists(\"/apps\"):\n        #     raise ValueError(\n        #         \"The apps directory does not exist. Please ensure the apps are mounted to /apps.\"\n        #     )\n\n    def install(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Run git pull and install to the target directory\n        \"\"\"\n        # ShowPrep Task called DeliveryTemplate\n        delivery_task = {\"code\": \"DeliveryTemplate\", \"parent\": None, \"type\": 1140}\n        luna_url = \"http://luna:8000/\"\n        meta_url = f\"{luna_url}/meta\"\n\n        response = self.get_task(url=meta_url, task=delivery_task)\n        status_code = response.status_code\n\n        if status_code == 200 and not response.json():\n            response = self.create_task(url=meta_url, task=delivery_task)\n        print(response.status_code)\n\n        # handler = plugins()\n        # handler.run_plugin(\n        #     \"plugin\",\n        #     \"Pixelfudger v3.2\",\n        #     allow_failure=False,\n        #     destination=\"/pipe/nuke/external/\",\n        # )\n\n        # handler.run_plugin(\n        #     \"plugin\",\n        #     \"Kdenlive Installer\",\n        #     allow_failure=False,\n        #     destination=\"/apps/kdenlive\",\n        # )\n        #\n        # handler.run_plugin(\n        #     \"plugin\",\n        #     \"Blender Installer\",\n        #     allow_failure=False,\n        #     destination=\"/apps/blender\",\n        #     version=\"4.2.0\",\n        # )\n        #\n        # handler.run_plugin(\n        #     \"plugin\",\n        #     \"PyCharm Installer\",\n        #     allow_failure=False,\n        #     destination=\"/apps/pycharm\",\n        #     version=\"2024.1.4\",\n        # )\n        #\n        # handler.run_plugin(\n        #     \"plugin\", \"ComfyUI Installer\", allow_failure=False, destination=\"/apps/comfyui\"\n        # )\n        #\n        # handler.run_plugin(\n        #     \"plugin\",\n        #     \"Nuke Installer\",\n        #     allow_failure=False,\n        #     destination=\"/apps/nuke\",\n        #     version=\"Nuke15.1v1\",\n        # )\n\n    def get_task(self, url, task):\n        \"\"\"\n        get a task from luna\n        \"\"\"\n        url = f\"{url}/filter\"\n        response = request(\"post\", url, json=task)\n        return response\n\n    def create_task(self, url, task):\n        \"\"\"\n        create a task in luna\n        \"\"\"\n        print(\"CREATING TASK\")\n        task[\"metadata\"] = {\"TemplateType\": \"Delivery\"}\n        print(task)\n        return request(\"post\", url, json=task)\n\n    def uninstall(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Uninstall the application.\n        \"\"\"\n        self.logger.info(\"Uninstalling not implemented\")\n</code></pre>"},{"location":"bundles/#testing-the-plugin","title":"Testing the Plugin","text":"<p>Now that we have written our plugin, we can test it.</p> <p>Note: TESTS ARE NOT OPTIONAL! You must write tests for your plugin to run.</p>"},{"location":"bundles/#writing-test-runner","title":"Writing Test Runner","text":"<p>We will write a test runner that will run our plugin.</p> tests/test_juno_pipeline.py<pre><code>\"\"\"\nTests for juno_pipeline\n\"\"\"\nimport requests\nfrom terra.loaders import plugins\n\n\ndef test_juno_pipeline():\n    \"\"\"\n    Test juno_pipeline installer.\n</code></pre>"},{"location":"bundles/#loading-our-plugin","title":"Loading our Plugin","text":"<p>We will load our plugin and then run it with the field values and <code>_alias_</code> we provided above.</p> Loading Plugin<pre><code>    \"\"\"\n    handler = plugins()\n    plugin = handler.get_plugin(\"plugin\", \"Juno Pipeline (Full)\")\n    assert plugin is not None\n    assert plugin._version_ is not None\n    handler.run_plugin(\"Juno Pipeline (Full)\", allow_failure=False)\n\n    delivery_task = {\"code\": \"DeliveryTemplate\", \"parent\": None, \"type\": 1040}\n    luna_url = \"http://luna:8000/\"\n    meta_url = f\"{luna_url}/meta/filter\"\n    response = requests.post(f\"{meta_url}\", json=delivery_task, timeout=600)\n    assert type(response.json()) == list\n    assert len(response.json()) == 1\n</code></pre>"},{"location":"bundles/#running-the-test","title":"Running the Test","text":"<pre><code>make dev-juno_pipeline\n</code></pre> <p>We can then check the install with the following command from the root of the repository.</p> <pre><code>ls -la .apps/\n</code></pre>"},{"location":"bundles/#conclusion","title":"Conclusion","text":"<p>You have now created a simple Bundle which can be used to install multiple plugins at once. This gives you a ton of flexibility in how you can install software on your Orion cluster.</p>"},{"location":"getting_started/","title":"Writing Our First Plugin","text":""},{"location":"getting_started/#prerequisites","title":"Prerequisites","text":"<p>Note: While you can use Windows, we prefer you use a Linux environment for development.</p> <ol> <li> <p>Clone the Terra repository <pre><code>git clone https://github.com/juno-fx/Terra-Official-Plugins.git\n</code></pre></p> </li> <li> <p>Change directory to the Terra repository <pre><code>cd Terra-Official-Plugins\n</code></pre></p> </li> </ol> <p>Once here, you need to have the following installed on your system:</p> <ul> <li>Python 3.12 - Programming language</li> <li>Docker - Containerization platform</li> <li>kind - Kubernetes in Docker</li> <li>skaffold - Kubernetes development tool</li> <li>kubectl - Kubernetes command-line tool</li> <li>make - Build automation tool</li> </ul> <p>Terra ships with the fully configured development environment, so you can get started quickly.  If you are curious, you can take a look in the <code>k8s</code> directory at the root of this repository.</p>"},{"location":"getting_started/#preparing-development-environment","title":"Preparing Development Environment","text":"<ol> <li>Setup your virtual environment and install all dependencies <pre><code>make install\n</code></pre></li> </ol>"},{"location":"getting_started/#creating-a-new-plugin-from-template","title":"Creating a New Plugin from Template","text":"<p>The repository comes with a template plugin that you can use to get started. To create a new plugin from the template, you can run the following command:</p> <p><pre><code>make app-houdini22.5\n</code></pre> The make command will copy the template plugins structure in all required directories and rename the files to match the new plugin name.</p> <p>The plugins files will need to be edited to match the new plugin name and description.</p> <p>Check the plugin python files in plugins/ and plugins/scripts directories and update the plugin name and description. Add the links or fields necesary for the plugin to work. Change the shell script in the plugins/scripts directory to do what your app would need to do like in a regular shell session.</p> <p>Usualy there is a download link to curl or wget and a destination directory to install the downloaded file passed from python to shell script.</p> <p>Add an icon to the plugins directory with the name of the plugin and the extension .png. This icon will be used in the Terra UI. You can provide an url to an image or use a local image. The plugin will be ready to be tested after the changes are made.</p> <p>You are not bound to this template by any means, this just show the easy way to create a new plugin from template with boilerplate plugin code. Instead sh scripts, you could be using python scripts or any other language that you are comfortable with and its available in the Terra environment.</p>"},{"location":"getting_started/#creating-a-new-plugin-from-scratch","title":"Creating a New Plugin from scratch","text":"<p>We are going to create a simple plugin that will clone a repository from a git repository to a specified directory. We will make it as  simple as possible. and very flexible so it can be used as a component for other plugins.</p>"},{"location":"getting_started/#the-plugin-directory","title":"The <code>plugin</code> Directory","text":"<p>The <code>plugin</code> directory is where all your plugins will live. We use pluginlib to manage our plugins and recursively load them. </p> <ol> <li>Create a new python file in the <code>plugin</code> directory <pre><code>touch plugins/gitloader.py\n</code></pre></li> </ol>"},{"location":"getting_started/#setup-run-environment","title":"Setup Run Environment","text":"<p>When Terra runs a plugin, it is launched as a Kubernetes Job. That job will load your plugin and then execute it with pytest.</p> <p>Note: Terra uses pytest to run your plugin also in production. We mainly do this because of how much extra information pytest provides for debugging purposes.</p> <ol> <li>Create a new python file in the <code>tests</code> directory <pre><code>touch tests/test_gitloader.py\n</code></pre></li> </ol>"},{"location":"getting_started/#writing-the-plugin","title":"Writing the Plugin","text":"<p>Now that we have our plugin file, we can start writing our plugin.</p> plugins/gitloader.py<pre><code>\"\"\"\nCloning down a repository from a Git Source to a target directory.\n\"\"\"\n\n# std\nimport os\nfrom pathlib import Path\n\n# 3rd\nfrom git import Repo\nfrom terra import Plugin\n\nclass GitLoader(Plugin):\n    \"\"\"\n    Git Loader\n    \"\"\"\n</code></pre>"},{"location":"getting_started/#configuration","title":"Configuration","text":"<p>Plugins must define certain information about it to be picked up by Terra.</p> <ul> <li><code>_alias_</code> - The name of the plugin</li> <li><code>icon</code> - The icon to display in the UI. This should be a URL to an image.</li> <li><code>description</code> - A short description of what the plugin does.</li> <li><code>category</code> - The category the plugin belongs to. This is used to group plugins in the UI and also provides insight into what industry this plugin may be used for.</li> <li><code>tags</code> - A list of tags that can be used to search for the plugin in the UI.</li> <li><code>fields</code> - A list of fields that the plugin uses as input. This is used to generate the UI for the plugin and get input from the user.<ul> <li><code>Plugin.field</code> - This is a data set that describes the input field<ul> <li><code>Reference Name</code> - The name of the field which is used to parse from the frontend</li> <li><code>Display Name</code> - The name of the field which is displayed to the user</li> <li><code>required</code> - If the field is required</li> </ul> </li> </ul> </li> </ul> Configuration<pre><code>    _version_ = \"1.0.0\"\n    _alias_ = \"Git Loader\"\n    icon = \"https://git-scm.com/images/logos/downloads/Git-Icon-1788C.png\"\n    description = \"Clone down a repository from a Git Source\"\n    category = \"Utility\"\n    tags = [\"git\", \"loader\", \"repository\", \"clone\"]\n    fields = [\n        Plugin.field(\"url\", \"Git URL\", required=True),\n        Plugin.field(\"ref\", \"Git Ref\", required=True),\n        Plugin.field(\"destination\", \"Destination directory\", required=True),\n</code></pre>"},{"location":"getting_started/#preflight","title":"Preflight","text":"<p>Next we will add a <code>preflight</code> function which will validate the input from  the user and ensure that the plugin can run successfully.</p> Preflight<pre><code>    def preflight(self, *args, **kwargs) -&gt; bool:\n        \"\"\"\n        Check if the target directory exists\n        \"\"\"\n        # store on instance\n        self.url = kwargs.get(\"url\")\n        self.ref = kwargs.get(\"ref\")\n        self.destination = kwargs.get(\"destination\")\n\n        # validate\n        if not self.url:\n            raise ValueError(\"No git url provided\")\n\n        if not self.ref:\n            raise ValueError(\"No git ref provided\")\n\n        if not self.destination:\n            raise ValueError(\"No destination directory provided\")\n\n        if not self.destination.endswith(\"/\"):\n</code></pre>"},{"location":"getting_started/#install","title":"Install","text":"<p>Lastly, we will add the <code>install</code> function which will execute the plugin.</p> Install<pre><code>    def install(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Run git pull and install to the target directory\n        \"\"\"\n        self.logger.info(f\"Cloning {self.url} to {self.destination}\")\n        if not os.path.exists(self.destination):\n            Repo.clone_from(self.url, self.destination)\n        self.logger.info(f\"Checking out {self.ref}\")\n        repo = Repo(self.destination)\n</code></pre>"},{"location":"getting_started/#final-code","title":"Final Code","text":"Final Plugin Code<pre><code>\"\"\"\nCloning down a repository from a Git Source to a target directory.\n\"\"\"\n\n# std\nimport os\nfrom pathlib import Path\n\n# 3rd\nfrom git import Repo\nfrom terra import Plugin\n\nclass GitLoader(Plugin):\n    \"\"\"\n    Git Loader\n    \"\"\"\n\n    _version_ = \"1.0.0\"\n    _alias_ = \"Git Loader\"\n    icon = \"https://git-scm.com/images/logos/downloads/Git-Icon-1788C.png\"\n    description = \"Clone down a repository from a Git Source\"\n    category = \"Utility\"\n    tags = [\"git\", \"loader\", \"repository\", \"clone\"]\n    fields = [\n        Plugin.field(\"url\", \"Git URL\", required=True),\n        Plugin.field(\"ref\", \"Git Ref\", required=True),\n        Plugin.field(\"destination\", \"Destination directory\", required=True),\n    ]\n\n    def preflight(self, *args, **kwargs) -&gt; bool:\n        \"\"\"\n        Check if the target directory exists\n        \"\"\"\n        # store on instance\n        self.url = kwargs.get(\"url\")\n        self.ref = kwargs.get(\"ref\")\n        self.destination = kwargs.get(\"destination\")\n\n        # validate\n        if not self.url:\n            raise ValueError(\"No git url provided\")\n\n        if not self.ref:\n            raise ValueError(\"No git ref provided\")\n\n        if not self.destination:\n            raise ValueError(\"No destination directory provided\")\n\n        if not self.destination.endswith(\"/\"):\n            self.destination += \"/\"\n\n    def install(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Run git pull and install to the target directory\n        \"\"\"\n        self.logger.info(f\"Cloning {self.url} to {self.destination}\")\n        if not os.path.exists(self.destination):\n            Repo.clone_from(self.url, self.destination)\n        self.logger.info(f\"Checking out {self.ref}\")\n        repo = Repo(self.destination)\n        repo.git.checkout(self.ref)\n\n        self.update_metadata({\"install_location\": self.destination})\n\n    def uninstall(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Uninstall the plugin.\n        \"\"\"\n        self.logger.info(f\"Removing {self._alias_}\")\n        self.destination = Path(kwargs.get(\"destination\")).as_posix()\n        if (\n                run(\n                    f\"rm -rf {self.destination}\",\n                    shell=True,\n                    check=False,\n                ).returncode\n                != 0\n        ):\n            raise RuntimeError(f\"Failed to remove {self._alias_}. Please read trough the logs and try to manually remove it.\")\n\n        else:\n            self.logger.info(f\"Successfully removed {self._alias_} plugin.\")\n</code></pre>"},{"location":"getting_started/#testing-the-plugin","title":"Testing the Plugin","text":"<p>Now that we have written our plugin, we can test it.</p> <p>Note: TESTS ARE NOT OPTIONAL! You must write tests for your plugin to run.</p>"},{"location":"getting_started/#writing-test-runner","title":"Writing Test Runner","text":"<p>We will write a test runner that will run our plugin.</p> tests/test_gitloader.py<pre><code>\"\"\"\nTests for gitloader\n\"\"\"\nfrom os import listdir\nfrom terra.loaders import plugins\n\n\ndef test_gitloader():\n    \"\"\"\n    Verify that we can even use this endpoint\n    \"\"\"\n</code></pre>"},{"location":"getting_started/#loading-our-plugin","title":"Loading our Plugin","text":"<p>We will load our plugin and then run it with the field values and <code>_alias_</code> we provided above.</p> Loading Plugin<pre><code>def test_gitloader():\n    \"\"\"\n    Verify that we can even use this endpoint\n    \"\"\"\n    handler = plugins()\n    plugin = handler.get_plugin(\"plugin\", \"Git Loader\")\n    assert plugin is not None\n    assert plugin._version_ is not None\n    handler.run_plugin(\n        \"Git Loader\",\n        allow_failure=False,\n        url=\"https://github.com/juno-fx/Terra-Official-Plugins.git\",\n        ref=\"main\",\n        destination=\"/tmp/terra\",\n    )\n</code></pre> <p>Note: This matches the exact same way the production environment will run your plugin.</p>"},{"location":"getting_started/#running-the-test","title":"Running the Test","text":"<p>Now that we have our entire environment setup, it is time to run the test. We provide a <code>Makefile</code> target that will run the test for you. Then naming convention for the run is <code>make dev-&lt;test name&gt;</code>. In our case, we called our test <code>test_gitloader</code>, so <code>make</code> will target your test env and run it for you.</p> <pre><code>make dev-gitloader\n</code></pre> <p>This will do a few things in this order:</p> <ol> <li>Launch a <code>kind</code> cluster</li> <li>Use <code>skaffold</code> to build the runner container</li> <li><code>skaffold</code> will then deploy the <code>k8s</code> folder to the kind cluster</li> <li>Using the <code>skaffold</code> verify configuration, it will run your plugin in the kind cluster as a job, just like it does in production.</li> <li>The runner will then load your plugin and execute it in a <code>pytest</code> environment in full debug mode.</li> <li>If everything is successful, you will see a <code>PASSED</code> message at the end of the output.</li> </ol>"},{"location":"getting_started/#cleaning-up","title":"Cleaning Up","text":"<p>Once you are done testing, you can clean up your environment by running the following command:</p> <pre><code>make down\n</code></pre>"},{"location":"getting_started/#congratulations","title":"Congratulations!","text":"<p>You have written your first Terra plugin. You can now start building more complex plugins and contributing to the Terra  ecosystem. Take a look at some more of our Official Juno Plugins to get an idea of what you can build.</p>"},{"location":"getting_started/#next-steps","title":"Next Steps","text":"<ul> <li>Software Installers</li> <li>Extensions</li> <li>Service Installers</li> <li>Bundles</li> </ul>"},{"location":"software_installers/","title":"Writing Software Installers","text":""},{"location":"software_installers/#prerequisites","title":"Prerequisites","text":"<ul> <li>Read Getting Started</li> <li>Enough Local Storage to install Blender</li> </ul>"},{"location":"software_installers/#creating-a-blender-installer","title":"Creating a Blender Installer","text":"<p>We are going to create a simple installer which will automate the entire install process for Blender.</p>"},{"location":"software_installers/#the-plugin-directory","title":"The <code>plugin</code> Directory","text":"<ul> <li>Create a new python file in the <code>plugin</code> directory <pre><code>touch plugins/blender.py\n</code></pre></li> <li>We will also be using a shell script to do the install. Create a shell script under <code>plugins/scripts</code> directory <pre><code>touch plugins/scripts/blender-installer.sh\n</code></pre></li> </ul>"},{"location":"software_installers/#setup-run-environment","title":"Setup Run Environment","text":"<ul> <li>Create a new python file in the <code>tests</code> directory <pre><code>touch tests/test_blender.py\n</code></pre></li> </ul>"},{"location":"software_installers/#writing-the-plugin","title":"Writing the Plugin","text":"<p>Now that we have our plugin file, we can start writing our plugin.</p> plugins/blender.py<pre><code>\"\"\"\nHandles installing Blender\n\"\"\"\n\n# std\nimport os\nfrom subprocess import run\nfrom pathlib import Path\n\n# 3rd\nfrom terra import Plugin\n\n\nclass BlenderInstaller(Plugin):\n    \"\"\"\n    Blender\n</code></pre>"},{"location":"software_installers/#configuration","title":"Configuration","text":"Configuration<pre><code>    _version_ = \"1.0.0\"\n    _alias_ = \"Blender Installer\"\n    icon = \"https://github.com/juno-fx/Terra-Official-Plugins/blob/main/plugins/assets/blender.png?raw=true\"\n    description = \"Blender is licensed as GNU GPL, owned by its contributors. For that reason Blender is Free and Open Source software, forever.\"\n    category = \"Applications\"\n    tags = [\n        \"3d\",\n        \"animation\",\n        \"vfx\",\n</code></pre>"},{"location":"software_installers/#preflight","title":"Preflight","text":"<p>Next we will add a <code>preflight</code> function which will validate the input from the user and ensure that the plugin can run successfully.</p> Preflight<pre><code>        \"blender\",\n        \"visual effects\",\n        \"rendering\",\n        \"cg\",\n        \"modeling\",\n    ]\n    fields = [\n        Plugin.field(\"version\", \"Version of Blender to install. i.e. 4.2.0\", required=False),\n        Plugin.field(\"destination\", \"Destination directory\", required=True),\n    ]\n\n    def preflight(self, *args, **kwargs) -&gt; bool:\n        \"\"\"\n        Check if the target directory exists and validate the arguments passed.\n        \"\"\"\n        # store on instance\n</code></pre>"},{"location":"software_installers/#install","title":"Install","text":"<p>Lastly, we will add the <code>install</code> function which will execute the plugin.</p> Install<pre><code>        self.destination = Path(kwargs.get(\"destination\")).as_posix()\n        self.executable = Path(self.destination).joinpath(\"latest\").as_posix()\n\n        # validate\n        if not self.destination:\n            raise ValueError(\"No destination directory provided\")\n\n        os.makedirs(self.destination, exist_ok=True)\n\n    def install(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Download and unpack the PyCharm.\n        \"\"\"\n        scripts_directory = os.path.abspath(f\"{__file__}/../scripts\")\n        self.logger.info(f\"Loading scripts from {scripts_directory}\")\n        if (\n            run(\n                f\"bash {scripts_directory}/blender-installer.sh {self.version} {self.destination}\",\n                shell=True,\n                check=False,\n            ).returncode\n            != 0\n        ):\n            raise RuntimeError(\"Failed to install Blender\")\n\n    def uninstall(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Uninstall the plugin.\n        \"\"\"\n        self.logger.info(f\"Removing {self._alias_}\")\n        self.destination = Path(kwargs.get(\"destination\")).as_posix()\n        if (\n                run(\n                    f\"rm -rf {self.destination}\",\n                    shell=True,\n                    check=False,\n                ).returncode\n                != 0\n        ):\n            raise RuntimeError(f\"Failed to remove {self._alias_}. Please read trough the logs and try to manually remove it.\")\n\n        else:\n            self.logger.info(f\"Successfully removed {self._alias_} plugin.\")\n</code></pre>"},{"location":"software_installers/#shelling-out","title":"Shelling Out","text":"<p>In the case of Blender (and many other software installers), it is actually easier to shell out to the command line to install the software. We will do  that with pythons <code>subprocess</code> module.</p> <p>Install<pre><code>        self.destination = Path(kwargs.get(\"destination\")).as_posix()\n        self.executable = Path(self.destination).joinpath(\"latest\").as_posix()\n\n        # validate\n        if not self.destination:\n            raise ValueError(\"No destination directory provided\")\n\n        os.makedirs(self.destination, exist_ok=True)\n\n    def install(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Download and unpack the PyCharm.\n        \"\"\"\n        scripts_directory = os.path.abspath(f\"{__file__}/../scripts\")\n        self.logger.info(f\"Loading scripts from {scripts_directory}\")\n        if (\n            run(\n                f\"bash {scripts_directory}/blender-installer.sh {self.version} {self.destination}\",\n                shell=True,\n                check=False,\n            ).returncode\n            != 0\n        ):\n            raise RuntimeError(\"Failed to install Blender\")\n\n    def uninstall(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Uninstall the plugin.\n        \"\"\"\n        self.logger.info(f\"Removing {self._alias_}\")\n        self.destination = Path(kwargs.get(\"destination\")).as_posix()\n        if (\n                run(\n                    f\"rm -rf {self.destination}\",\n                    shell=True,\n                    check=False,\n                ).returncode\n                != 0\n        ):\n            raise RuntimeError(f\"Failed to remove {self._alias_}. Please read trough the logs and try to manually remove it.\")\n\n        else:\n            self.logger.info(f\"Successfully removed {self._alias_} plugin.\")\n</code></pre> This will then call a bash script that will install Blender based on the  arguments passed to it via python.</p> plugins/scripts/blender-installer.sh<pre><code>echo \"Installing Blender $1 - $2 ...\"\n\n# split 4.2.0 to 4.2\nversion=$(echo $1 | cut -d'.' -f1-2)\n\nif [ \"$DEV_APPS_DEBUG\" = true ]\nthen\n  cd /tmp\nelse\n  echo \"Downloading Blender...\"\n  wget -q -O /tmp/blender-$1-linux-x64.tar.xz \"https://mirror.clarkson.edu/blender/release/Blender$version/blender-$1-linux-x64.tar.xz\"\nfi\n\necho \"Extracting Blender...\"\ntar -xvf /tmp/blender-$1-linux-x64.tar.xz -C \"$2\"/ &gt; /dev/null\n# blender-$1-linux-x64\nln -sfv \"$2/blender-$1-linux-x64/blender\" \"$2/latest\"\n\n\nSCRIPT_DIR=$( cd -- \"$( dirname -- \"${BASH_SOURCE[0]}\" )\" &amp;&gt; /dev/null &amp;&amp; pwd )\ncp -v \"$SCRIPT_DIR/blender.sh\" \"$2/\"\nsed -i \"s@ROOT_APP@$2/blender-$1-linux-x64@g\" \"$2/blender.sh\"\nchmod +x \"$2/blender.sh\"\n\n# app icon setup\ncd $SCRIPT_DIR\ncp \"../assets/blender.png\" \"$2/blender.png\"\necho \"Adding desktop file\"\nchmod +X create_desktop_file.py\npython3 create_desktop_file.py --app_name=\"Blender\" --version=$version --latest_path=\"$2\"/blender.sh --categories=\"blender, render\" --destination=\"$2\" --icon=\"$2\"/blender.png\necho \"Desktop file created.\"\nchmod -R 777 \"$2/\"\ncat $2/*.desktop\n</code></pre>"},{"location":"software_installers/#final-code","title":"Final Code","text":"Final Plugin Code<pre><code>\"\"\"\nHandles installing Blender\n\"\"\"\n\n# std\nimport os\nfrom subprocess import run\nfrom pathlib import Path\n\n# 3rd\nfrom terra import Plugin\n\n\nclass BlenderInstaller(Plugin):\n    \"\"\"\n    Blender\n    \"\"\"\n\n    _version_ = \"1.0.0\"\n    _alias_ = \"Blender Installer\"\n    icon = \"https://github.com/juno-fx/Terra-Official-Plugins/blob/main/plugins/assets/blender.png?raw=true\"\n    description = \"Blender is licensed as GNU GPL, owned by its contributors. For that reason Blender is Free and Open Source software, forever.\"\n    category = \"Applications\"\n    tags = [\n        \"3d\",\n        \"animation\",\n        \"vfx\",\n        \"blender\",\n        \"visual effects\",\n        \"rendering\",\n        \"cg\",\n        \"modeling\",\n    ]\n    fields = [\n        Plugin.field(\"version\", \"Version of Blender to install. i.e. 4.2.0\", required=False),\n        Plugin.field(\"destination\", \"Destination directory\", required=True),\n    ]\n\n    def preflight(self, *args, **kwargs) -&gt; bool:\n        \"\"\"\n        Check if the target directory exists and validate the arguments passed.\n        \"\"\"\n        # store on instance\n        self.version = kwargs.get(\"version\", \"4.2.0\")\n        self.destination = Path(kwargs.get(\"destination\")).as_posix()\n        self.executable = Path(self.destination).joinpath(\"latest\").as_posix()\n\n        # validate\n        if not self.destination:\n            raise ValueError(\"No destination directory provided\")\n\n        os.makedirs(self.destination, exist_ok=True)\n\n    def install(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Download and unpack the PyCharm.\n        \"\"\"\n        scripts_directory = os.path.abspath(f\"{__file__}/../scripts\")\n        self.logger.info(f\"Loading scripts from {scripts_directory}\")\n        if (\n            run(\n                f\"bash {scripts_directory}/blender-installer.sh {self.version} {self.destination}\",\n                shell=True,\n                check=False,\n            ).returncode\n            != 0\n        ):\n            raise RuntimeError(\"Failed to install Blender\")\n\n    def uninstall(self, *args, **kwargs) -&gt; None:\n        \"\"\"\n        Uninstall the plugin.\n        \"\"\"\n        self.logger.info(f\"Removing {self._alias_}\")\n        self.destination = Path(kwargs.get(\"destination\")).as_posix()\n        if (\n                run(\n                    f\"rm -rf {self.destination}\",\n                    shell=True,\n                    check=False,\n                ).returncode\n                != 0\n        ):\n            raise RuntimeError(f\"Failed to remove {self._alias_}. Please read trough the logs and try to manually remove it.\")\n\n        else:\n            self.logger.info(f\"Successfully removed {self._alias_} plugin.\")\n</code></pre>"},{"location":"software_installers/#testing-the-plugin","title":"Testing the Plugin","text":"<p>Now that we have written our plugin, we can test it.</p> <p>Note: TESTS ARE NOT OPTIONAL! You must write tests for your plugin to run.</p>"},{"location":"software_installers/#writing-test-runner","title":"Writing Test Runner","text":"<p>We will write a test runner that will run our plugin.</p> tests/test_gitloader.py<pre><code>\"\"\"\nTests for blender\n\"\"\"\nfrom terra.loaders import plugins\n\n\ndef test_blender():\n    \"\"\"\n    Test blender installer.\n    \"\"\"\n</code></pre>"},{"location":"software_installers/#loading-our-plugin","title":"Loading our Plugin","text":"<p>We will load our plugin and then run it with the field values and <code>_alias_</code> we provided above.</p> Loading Plugin<pre><code>    handler = plugins()\n    plugin = handler.get_plugin(\"plugin\", \"Blender Installer\")\n    assert plugin is not None\n    assert plugin._version_ is not None\n    handler.run_plugin(\n        \"Blender Installer\",\n        allow_failure=False,\n        destination=\"/apps/blender\",\n        version=\"4.2.1\",\n    )\n\n    # test removal\n    handler.remove_plugin(name=\"Blender Installer\", destination=\"/apps/blender\")\n</code></pre> <p>Note: The <code>/apps</code> directory is mounted to the runner container and routed to repositories <code>.apps/</code> directory. This is very useful when you want to check the output of your installer. You can see we use this trick in this test  on line 8.</p> /apps<pre><code>         allow_failure=False,\n         destination=\"/apps/blender\",\n         version=\"4.2.1\",\n</code></pre>"},{"location":"software_installers/#running-the-test","title":"Running the Test","text":"<pre><code>make dev-blender\n</code></pre> <p>We can then check the install with the following command from the root of the repository.</p> <pre><code>ls -la .apps/blender\n</code></pre>"},{"location":"software_installers/#conclusion","title":"Conclusion","text":"<p>You have now created a simple installer for Blender. This can be expanded to any  software that you would like to install on your Orion cluster. We currently support  the following.</p> <ul> <li>Blender</li> <li>kdenlive</li> <li>PyCharm</li> <li>Nuke</li> <li>ComfyUI</li> <li>and many more to come...</li> </ul>"},{"location":"software_installers/#next-steps","title":"Next Steps","text":"<ul> <li>Extensions</li> <li>Service Installers</li> <li>Bundles</li> </ul>"},{"location":"using_plugins/","title":"Running A Terra Plugin","text":""},{"location":"using_plugins/#prerequisites","title":"Prerequisites","text":"<ul> <li>A running Orion Cluster</li> </ul>"},{"location":"using_plugins/#accessing-terra","title":"Accessing Terra","text":"<ul> <li>To access Terra, you will need to access the Orion Cluster Settings.</li> </ul> <ul> <li>From there, you should see \"Marketplace\" at the bottom of the settings list.</li> </ul> <ul> <li>Click on \"Marketplace\" and you will be taken to the \"Terra Marketplace\".</li> </ul>"},{"location":"using_plugins/#install-blender","title":"Install Blender","text":"<ul> <li>Click the \"Get Started\" button under the Blender logo in the \"Default Extensions\" section of the Market.</li> </ul> <ul> <li>Fill out all the required fields and click \"Install\" at the bottom of the Plugin.</li> </ul>"},{"location":"using_plugins/#check-installation","title":"Check Installation","text":"<p>You can check the install logs and progress by clicking the Puzzle Piece icon in the top right of the Market. That will take you to the \"Sources and Installed\" page.</p> <p></p> <p>From here, you can check the state of your install, get logs and much more.</p>"},{"location":"using_plugins/#custom-sources","title":"Custom Sources","text":"<p>You can add additional sources to the Terra Marketplace by clicking the \"New Source\" button in the top left of the page. As long as the repository follows the same structure as the Terra-Official-Plugins, you can add it to your Orion Cluster.</p>"}]}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}-promox
spec:
  selector:
    juno-innovations.com/workstation: "{{ .Values.name }}"
  ports:
    - port: 8006
      targetPort: 8006
      protocol: TCP
      name: webui

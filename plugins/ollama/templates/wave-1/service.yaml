apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
spec:
  ports:
    - port: 11434
      targetPort: 11434
      protocol: TCP
      name: api
  selector:
    terra.juno-innovations.com/app: "{{ .Release.Name }}"
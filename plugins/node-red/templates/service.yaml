apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
spec:
  selector:
    juno-innovations.com/dashboard: "{{ .Release.Name }}"
  ports:
    - port: 1880
      targetPort: 1880
      protocol: TCP
      name: ui

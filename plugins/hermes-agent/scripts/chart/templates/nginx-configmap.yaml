apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-nginx
  labels:
    kuiper.juno-innovations.com/chart: "{{ .Release.Name }}-scripts-configmap"
data:
  nginx.conf: |
{{ tpl (.Files.Get "files/nginx/nginx.conf") . | indent 4 }}

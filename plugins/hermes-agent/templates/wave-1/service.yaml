apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
spec:
  type: ClusterIP
  ports:
    - port: 8642
      targetPort: 8642
      protocol: TCP
      name: api
  selector:
    terra.juno-innovations.com/app: "{{ .Release.Name }}"

apiVersion: v1
data:
  tls.crt: "{{ .Values.certificate }}"
  tls.key: "{{ .Values.key }}"
kind: Secret
metadata:
  name: ingress-certificate
  namespace: "{{ .Values.project }}"
type: kubernetes.io/tls

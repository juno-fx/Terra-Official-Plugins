apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ .Release.Name }}-network-policy
spec:
  podSelector:
    matchLabels:
      terra.juno-innovations.com/tdk: "true"
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx

apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-grafana-proxy
  namespace: prometheus
spec:
  selector:
    app: {{ .Release.Name }}-grafana-proxy
  ports:
    - port: 8080
      targetPort: 8080

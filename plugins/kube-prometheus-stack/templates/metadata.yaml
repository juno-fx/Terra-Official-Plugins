apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-terra-metadata
data:
  chart: "{{ .Chart.Name }}"
  ingress: "{{ .Values.path }}"
  {{- if .Values.adminOnly }}
  permission: admin
  {{- end }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ .Release.Name }}-nvidia-gpu-operator
  namespace: argocd
spec:
  project: default
  destination:
    server: https://kubernetes.default.svc
    namespace: gpu-operator
  sources:
    - repoURL: https://helm.ngc.nvidia.com/nvidia
      chart: gpu-operator
      targetRevision: {{ .Values.version }}
      helm:
        releaseName: gpu-operator
        parameters:
          - name: "toolkit.enabled"
            value: "{{ .Values.install_toolkit }}"
          - name: "driver.enabled"
            value: "{{ .Values.install_driver }}"
          - name: "driver.nvidiaDriverCRD.enabled"
            value: "{{ .Values.install_driver_crd }}"
          - name: "devicePlugin.config.name"
            value: "{{ .Values.slicing_config }}"
          - name: "devicePlugin.config.default"
            value: "{{ .Values.default_config }}"
          - name: "driver.useOpenKernelModules"
            value: "{{ .Values.open_kernel_modules }}"
          - name: "nfd.enabled"
            value: "{{ .Values.nfd_enabled }}"
          - name: "dcgm.enabled"
            value: "{{ .Values.dcgm_enabled }}"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: true
    syncOptions:
      - CreateNamespace=true

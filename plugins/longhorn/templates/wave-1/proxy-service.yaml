apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-longhorn-proxy
  namespace: longhorn-system
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: {{ .Release.Name }}-longhorn-proxy
  ports:
    - port: 8080
      targetPort: 8080

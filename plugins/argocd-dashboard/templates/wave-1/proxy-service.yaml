apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-argocd-proxy"
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: "{{ .Release.Name }}-argocd-proxy"
  ports:
    - port: 8080
      targetPort: 8080

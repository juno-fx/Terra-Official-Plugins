apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "{{ .Release.Name }}"
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: {{ .Values.storage_class }}
  resources:
    requests:
      storage: {{ .Values.storage_size }}

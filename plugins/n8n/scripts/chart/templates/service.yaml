apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
spec:
  selector:
    kuiper.juno-innovations.com/kuiper-instance: "{{ .Release.Name }}"
  ports:
    - port: 5678
      targetPort: 5678
      protocol: TCP
      name: ui
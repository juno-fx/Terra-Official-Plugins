apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
spec:
  selector:
    kuiper.juno-innovations.com/kuiper-instance: "{{ .Release.Name }}"
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: webtop
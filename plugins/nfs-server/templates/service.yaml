apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
spec:
  type: NodePort
  ports:
    - port: 2049
      targetPort: 2049
      protocol: TCP
      nodePort: {{ .Values.port }}
      name: nfs
  selector:
    app: "{{ .Release.Name }}"
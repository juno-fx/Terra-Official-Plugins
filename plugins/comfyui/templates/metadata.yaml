apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-terra-metadata"
  annotations:
    kuiper.juno-innovations.com/workload: "true"  # mark this as a workload for kuiper and front end UI reference
  labels:
    kuiper.juno-innovations.com/chart: "{{ .Release.Name }}-scripts-configmap"
data:
  chart: "{{ .Release.Name }}-scripts-configmap"
  description: "ComfyUI Deployment"
  fields: |
    []

apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Values.name }}"
data:
  entrypoint.sh: |
    echo "Changing directory"
    cd "{{ .Values.entrypoint }}"
    source .venv/bin/activate
    python3 main.py --listen 0.0.0.0

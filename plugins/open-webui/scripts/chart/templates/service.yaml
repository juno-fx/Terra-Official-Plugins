apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.name }}-open-webui"
spec:
  selector:
    juno-innovations.com/workstation: "{{ .Values.name }}"
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: webui

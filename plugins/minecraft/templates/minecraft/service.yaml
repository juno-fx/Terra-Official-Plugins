apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
spec:
  selector:
    juno-innovations.com/workload: "{{ .Release.Name }}"
  ports:
    - port: 25565
      targetPort: 25565
      protocol: TCP
      name: game
    - port: 25575
      targetPort: 25575
      protocol: TCP
      name: rcon

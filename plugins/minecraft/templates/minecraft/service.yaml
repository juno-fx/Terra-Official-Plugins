apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}"
  annotations:
    tailscale.com/expose: "true"
spec:
  selector:
    juno-innovations.com/workload: "{{ .Release.Name }}"
  ports:
    - port: 25565
      targetPort: 25565
      protocol: TCP
      name: game
